rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary: Allow all read/write operations for debugging
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 